<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;500;700&display=swap"
      rel="stylesheet"
    />
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN"
      crossorigin="anonymous"
    />

    <link rel="stylesheet" href="style.css" />
    <title><%= title %></title>
  </head>
  <body class="vh-100">
    <nav
      class="navbar bg-dark d-flex justify-content-space-between align-items-center"
    >
      <div class="align-items-center d-flex">
        <% if (user) { %>

        <small class="navbar-brand text-white mx-3 w-75"
          ><%= user.email %></small
        >
        <% }%>
      </div>

      <a href="/logout" class="btn btn-danger mx-3">LogOut</a>
    </nav>

    <main class="vw-100 h-75 p-3">
      <div
        id="messages"
        class="d-flex flex-column p-3 h-100 overflow-y-auto"
      ></div>

      <div
        class="position position-absolute bottom-0 start-0 end-0 d-flex flex-column align-items-center my-3 text"
      >
        <small id="typing" class="text-secondary my-1"></small>
        <div class="w-100 d-flex justify-content-center">
          <input id="input" class="w-75" type="text" placeholder="Message..." />
          <button id="submit" type="button" id="button-addon2">Send</button>
        </div>
      </div>
    </main>

    <script src="/socket.io/socket.io.js"></script>
    <script src="main.js"></script>
  </body>
</html>
